name: Build en DEV_JBCR

on:
  push:
    branches:
      - DEV_JBCR

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v2
      with:
        repository: juliobautistaSFTK/TallerGITHUB
        ref: DEV_JBCR

    - name: Build with Maven
      run: mvn clean -B package --file hola-mundo/pom.xml

    - name: Contenido del JAR
      run: jar tf target/hola-mundo-1.0-SNAPSHOT.jar

    - name: Ejecutar el JAR
      run: java -jar hola-mundo/target/hola-mundo-1.0-SNAPSHOT.jar      
