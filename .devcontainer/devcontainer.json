{
    "name": "Nginx Dev Container",
    "dockerComposeFile": "docker-compose.yml", // si usas docker-compose
    "service": "nginx", // nombre del servicio en docker-compose
    "settings": {},
    "forwardPorts": [80],
    "postCreateCommand": "apt-get update && apt-get install -y maven docker.io && systemctl enable docker && systemctl start docker",
    "remoteUser": "root"
}
